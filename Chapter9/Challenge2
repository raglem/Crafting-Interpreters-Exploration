fun forLoop(remainingIterations, callback) {
    if (remainingIterations <= 0) {
        return;
    }
    callback();
    remainingIterations = remainingIterations - 1;
    forLoop(remainingIterations, callback);
}

fun sayHi() {
    print "hello";
}

forLoop(5, sayHi);